import type { Metadata } from "next";
import { InfoPage } from "@/components/info-page";

export const metadata: Metadata = { title: "شرایط بازگشت", description: "ضمانت بازگشت هفت‌روزه و شرایط مرجوعی محصولات نُوین." };

export default function ReturnsPage() {
  return <InfoPage kicker="ضمانت نُوین" title="بازگشت ساده، بدون مسیر مبهم." intro="اگر محصول با سفارش شما مطابقت نداشته باشد، تا هفت روز امکان ثبت درخواست بررسی دارید." sections={[{title:"ثبت درخواست",body:"درخواست بازگشت باید حداکثر تا هفت روز پس از تحویل و همراه با شماره سفارش ثبت شود."},{title:"شرایط محصول",body:"کالا باید استفاده‌نشده، سالم و همراه با بسته‌بندی و متعلقات اصلی بازگردانده شود."},{title:"بررسی و بازپرداخت",body:"پس از دریافت و بررسی محصول، نتیجه اعلام و در صورت تأیید مبلغ از همان روش پرداخت بازگردانده می‌شود."}]}/>;
}
