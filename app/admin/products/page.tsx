import type { Metadata } from "next";
import Link from "next/link";
import { FormNotice } from "@/components/auth-shell";
import { DashboardHeader } from "@/components/dashboard-shell";
import { formatPrice } from "@/lib/catalog";
import { createClient } from "@/lib/supabase/server";
import { saveProductAction, toggleProductAction } from "@/app/admin/actions";
import { requireAdmin } from "@/lib/auth";

export const metadata: Metadata = { title: "مدیریت محصولات", robots: { index: false, follow: false } };
type Props = { searchParams: Promise<{ message?: string; error?: string }> };
export default async function AdminProductsPage({ searchParams }: Props) {
  const [, notice] = await Promise.all([requireAdmin(), searchParams]);
  const supabase = await createClient();
  const { data: products } = await supabase.from("products").select("id, name, slug, price, stock, is_active").order("created_at", { ascending: false });
  return <><DashboardHeader eyebrow="کاتالوگ" title="مدیریت محصولات" description="انتشار، موجودی و محتوای محصول را بدون تغییر کد کنترل کنید."/><FormNotice {...notice}/><section className="dashboard-panel"><div className="data-table-wrap"><table className="data-table"><thead><tr><th>محصول</th><th>قیمت</th><th>موجودی</th><th>وضعیت</th><th>عملیات</th></tr></thead><tbody>{products?.map((product) => <tr key={product.id}><td><strong>{product.name}</strong><small>{product.slug}</small></td><td>{formatPrice(product.price)}</td><td>{new Intl.NumberFormat("fa-IR").format(product.stock)}</td><td><span className={`status-pill ${product.is_active ? "active" : ""}`}>{product.is_active ? "منتشرشده" : "غیرفعال"}</span></td><td><div className="table-actions"><Link className="table-action" href={`/admin/products/${product.id}`}>ویرایش</Link><form action={toggleProductAction}><input type="hidden" name="id" value={product.id}/><input type="hidden" name="nextActive" value={String(!product.is_active)}/><button className="table-action" type="submit">{product.is_active ? "غیرفعال‌کردن" : "انتشار"}</button></form></div></td></tr>)}</tbody></table></div></section><section className="dashboard-panel" id="new-product"><h3>محصول جدید</h3><form className="panel-form product-form" action={saveProductAction}><div className="form-row"><label>نام فارسی<input name="name" required/></label><label>نام انگلیسی<input name="englishName" required/></label></div><div className="form-row"><label>Slug انگلیسی<input name="slug" dir="ltr" pattern="[a-z0-9]+(?:-[a-z0-9]+)*" required/></label><label>دسته‌بندی<select name="category" required><option>دیجیتال</option><option>اکسسوری</option><option>خانه و زندگی</option><option>مد روزمره</option></select></label></div><div className="form-row"><label>قیمت (تومان)<input name="price" type="number" min="0" required/></label><label>قیمت قبل از تخفیف<input name="compareAtPrice" type="number" min="0"/></label><label>موجودی<input name="stock" type="number" min="0" defaultValue="0" required/></label></div><div className="form-row"><label>نشان محصول<input name="badge" placeholder="مثلاً جدید"/></label><label>تصویرسازی<select name="artwork"><option value="headphones">هدفون</option><option value="watch">ساعت</option><option value="speaker">اسپیکر</option><option value="bag">کیف</option><option value="lamp">چراغ</option><option value="glasses">عینک</option><option value="mug">ماگ</option><option value="shoe">کفش</option></select></label></div><div className="form-row"><label>رنگ اصلی<input name="accent" type="color" defaultValue="#153f36"/></label><label>رنگ زمینه<input name="accentSoft" type="color" defaultValue="#dcefe6"/></label></div><label>توضیح کوتاه<textarea name="shortDescription" rows={2} required/></label><label>شرح کامل<textarea name="description" rows={5} required/></label><label>ویژگی‌ها، هر مورد در یک خط<textarea name="features" rows={5} required/></label><label className="checkbox-label"><input name="isActive" type="checkbox" defaultChecked/> انتشار در فروشگاه</label><button className="primary-button" type="submit">ذخیره و انتشار محصول</button></form></section></>;
}
